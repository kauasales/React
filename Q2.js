function inserir_num(){
    let num = parseFloat(window.prompt("Insira o numero: "))
    return num;
}

function cubo(a){
    c = Math.pow(a, 3)
    return window.alert(`Cubo de ${a}: ${c}`);
}