array = ['a', 'b', 'c']

function inverter(x){
    return window.alert(x.reverse());
}

function inver(x){

    let y = []

    for(i=x.length-1; i>=0; i--){

        y.push(x[i])
    }

    return window.alert(y);
}